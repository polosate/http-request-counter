version: '3'

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - FILENAME="requests.csv"
    volumes:
      - counter-data:/app

volumes:
  counter-data: